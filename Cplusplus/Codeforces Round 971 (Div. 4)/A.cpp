#include <bits/stdc++.h>

using namespace std;

#define endl '\n'
typedef long long ll;

int a,b;

void solve()
{
    cin >> a >> b;
    cout << b - a << endl;
}

int main()
{
    ios::sync_with_stdio(false);
	cin.tie(0); cout.tie(0);
    int t;
    cin >> t;
    while(t--)
    {
        solve();
    }

    return 0;
}